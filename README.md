# 长安十二时辰 · 灯火如昼

把马伯庸《长安十二时辰》改编成浏览器单页剧情冒险：覆盖完整十二时辰主线，**条件门控分支 + 4 结局 + MBTI 人格画像**。

零运行时依赖、零构建步骤 —— 纯原生 ES Modules，双击 `start.bat` 就能玩。

> ⚠️ **版权声明**：《长安十二时辰》原作著作权归马伯庸先生及相关权利人所有。本项目为非商业性质的学习与原型作品，**保留所有权利**，不得用于商业用途。若权利人认为不妥，请联系删除。

> ### ✅ 设定符合度声明（v6.0，已全面修正）
>
> **v6.0 已按「路线 A · 全面对齐原著」完成所有偏离修正**，逐处对原著小说与 2019 剧版「择优」取舍（决策见 [`docs/CANON.md`](docs/CANON.md) 第 0 节）。
> 修正覆盖 P0~P3 全部条目：龙波＝萧规（第八团同袍相认）、补第八团/烽燧堡、张小敬入狱真实原因（杀熊火帮 34 人＋斩县尉谭同寿＋劫持永王＋十恶·不义）、闻染立场还原（恨长安/被龙波利用）、龙波组织改「蚍蜉」（狼卫＝突厥右刹/曹破延/图格鲁为弃子）、灯楼改回兴庆宫（圣人在花萼相辉楼观灯）、鱼肠降回刺客、阙勒霍多补伏火雷＋延州石脂、补太子/永王/熊火帮/望楼旗语等。
> 4 个「人格参照」角色（何执正/林九郎/丁瞳儿/王蕴秀）按设计不进正文，已在工具里确认。
>
> 验收：自动化层（代码自洽 / 内部连贯 / 转场人物）全绿；设定符合度由人工核对＋CANON 基准表逐项判定，v6.0 已全部收敛。

---

## 运行

### 方式一：一键启动（推荐）

```bat
start.bat
```

脚本会依次检查 Python → 启动本地服务 → 等端口就绪 → 自动打开浏览器。

### 方式二：手动

```bash
python -m http.server 8010
# 浏览器打开 http://localhost:8010/
```

> **必须走 HTTP 服务**，不能直接双击 `index.html`。项目用原生 ES Modules，`file://` 协议下浏览器会因 CORS 策略拒绝加载模块。

也可以用 Node 起服务：`npx --yes serve . -l 8010`

---

## 玩法

- **顶栏十二时辰刻度**：当前时辰高亮，走过的自动点亮。
- 每个剧情节点含 2–4 个**关键抉择**（多数为 3 个，共 22 个剧情节点 / 66 个抉择），一个选项可以同时做这些事：
  触发跳转 / 打标记（flag）/ 获得线索 / 改变某角色信任度 / **为 6 个人格维度加分** / **为 MBTI 4 维加分**
- 选完之后，你会先走进一段**只属于这个选项的过场**，然后再汇合回主线。见下文「过场场景」。
- 走到 **亥时 · 抉择**（灯楼顶对峙龙波），根据你的 **同路人数** 与 **flag** 分流到 4 个结局之一。

### 过场场景（v5.4）

22 个抉择点里，**21 个**已经做到「每个选项把你带到不同的地方」；其中 **20 个**是「先走一段专属过场，再汇合回主干」。

过场是一个**新的节点类型**：非结局、**没有 `choices`**、只有一个 `next` 出口，引擎把它渲染成一个「继续」按钮。

```js
x_task_power: {
  id: 'x_task_power', hour: 'zi', place: '靖安司',
  pov: 'zhangxiaojing',
  text: `「给我人，给我权。我要在长安横着走。」
  李必没有立刻应。他看了你很久，那种看法你熟——是在算一个人值不值得放出去。
  最后他说了一个字：「准。」……`,
  next: 'h_zi2_gela',
  nextLabel: '走出靖安司。',
}
```

**过场不是抉择点**，这一点被三个地方同时保证（都走 `src/data/scenes.js` 导出的 `isPassage` / `nextTargets`，不许各工具自己抄一份）：

- 不推进 `state.choicesMade`，不累计 `traitRaw` / `mbtiRaw`
- 不参与 `nodeMax` / `mbtiNodeStats` 的**机会基线与满分**计算
- 不进 `branch_audit` 的「伪选择」分母

所以这一轮**没有新增任何抉择**（选项数仍是 66），人格与六维计分完全没动。

**写一段过场时的三条约定**（`tools/passage_audit.mjs` 会盯着前两条）：

1. **不复述下一场戏。** 过场写「选择那一刻之后、下一场戏之前」发生的事。如果过场把汇合点的句子先说了，汇合点读起来就是重复。
2. **不复述上游抉择点的正文。** 玩家上一屏刚读过，过场再念一遍 = 原地踏步。（这条是通读 59 段时手抓出来的：`x_doc_confront` 照抄了上游 `h_si_doc` 里 34 个字。）
3. **不复述下游的条件插段。** 插段的活是"接住"，过场抢了它的台词，插段一进来就撞车。

还有一个约束继承自主线的共享性：**过场与它所在的节点，都不能断言"你有什么 / 谁在场 / 谁还活着"这类路径级或结局级事实。** 这一轮据此删掉了 `x_tail_help` 里的一句「你身上没有腰牌，也没有官凭」——它和 `x_doc_confront` 的「把靖安司的令牌亮出来」在同一条时间线上直接对立。

### 四种结局

| 结局 | 触发条件 | 一句话 |
|---|---|---|
| **血色长安** | 拔刀，且同路人 **≥ 3** | 你用刀守住了长安。代价是你自己。 |
| **火起长安** | 拔刀，但同路人 **< 3**，且没毁掉油罐 | 你想一个人守住长安。长安没能守住。 |
| **灯火如昼** | 选「我不为长安死。我为这些人活。」且同路人 **≥ 4** | 你守住的不是一座城，是城里的人。 |
| **未竟之局** | 选「龙波，收手吧」且**查过朝堂**；或选「我懂你」且**龙波信任 ≥ 20** | 你阻止了火，却没能阻止长安慢慢烂掉。 |

**同路人**＝除主角张小敬外，信任度 ≥ `ALLY_TRUST`（75）的角色数。

门槛是分级设计而非一刀切：拔刀要 3 人（乱选的众数附近，跟着走就扛得住），最好的结局「灯火如昼」要 4 人（得真的把人拉到身边），「未竟之局」则完全不看人数、只看你有没有看清朝堂那条线——**或者**你有没有先听懂龙波恨的是什么。所以「独行到底」会输，「广结人脉」「追查朝堂」「以情动人」各通向一个隐藏结局。

还有一处**中段选择改变结局**的设计：如果你在 `h_mao_tail` 支路选了「摸进去，先毁掉这些油罐」，西市那十七罐阙勒霍多就少了一半——**哪怕同路人不足 3 人，拔刀也能守住**（条件路由 `{ when: { flagsAll: ['try_destroy'] }, then: 'e_final_fight' }`）。

实测（3000 局随机）结局分布：血色长安 49.7% / 未竟之局 24.1% / 灯火如昼 18.4% / 火起长安 7.9%。

---

## 条件系统（v4 核心）

这是让「选择真正影响结局」的机制。条件键在**选项的 `require`** 与**跳转路由的 `when`** 里通用，语义完全一致。

### 条件键

| 键 | 含义 |
|---|---|
| `flagsAll` | 这些 flag **全部**已设置 |
| `flagsAny` | 这些 flag **至少一个**已设置 |
| `flagsNone` | 这些 flag **一个都没有** |
| `trust` | 指定角色的信任度 **≥** 给定值，如 `{ trust: { longbo: 40 } }` |
| `allies` | 同路人（信任 ≥ 75 的非主角角色）**数量 ≥** 给定值 |
| `clues` | 已获线索数 **≥** 给定值 |

### 两种用法

**① 选项门控** —— `require` 不满足的选项不显示，改为渲染一条灰色「未解锁」提示（`requireHint`），让玩家知道错过了什么、怎么拿：

```js
{
  t: '「我不为长安死。我为这些人活。」',
  flag: 'final_people', to: 'e_final_people',
  require: { allies: 4 },
  requireHint: '你身后得真的站着人 —— 需要至少 4 位同路人信任你',
}
```

**② 条件路由** —— `to` 可以是字符串（固定跳转），也可以是规则数组，取**第一个** `when` 成立的 `then`：

```js
{
  t: '「我愿意。」——拔刀，拼死一战。',
  flag: 'final_fight',
  to: [
    { when: { allies: 3 }, then: 'e_final_fight' },  // 身后有人，刀才有意义
    { then: 'e_burn_fire' },                          // 兜底：孤身一人，拦不住他
  ],
}
```

当前共 **5 个条件选项 + 1 处条件路由**。

### 条件的第三种用法：条件正文

条件键还能用在**正文**上，让同一段剧情因你来路不同而不同。这是把 flag 变成叙事后果的最低成本方式——**不需要新增任何节点**。

**① `inserts`（首选）** —— 往默认正文里插入段落，可多条叠加：

```js
{
  id: 's1_task', ...,
  text: `李必展开一卷长安舆图，手指落在西市。

……「你果然知道。」李必收起图，「十二个时辰。」……

{{inserts}}

窗外的更鼓响了一声。子时已过。`,
  inserts: [
    {
      when: { flagsAll: ['probed_first'] },
      text: '「还找过谁？」你又问了一遍。\n\n李必沉默了一息。「三个。一个死在平康坊，一个跑了，一个不肯接。」',
    },
  ],
}
```

**插入点由正文里的 `{{inserts}}` 标记决定**——标记写在哪，插段就落在哪。

> ### ⚠️ 为什么不用「塞最前 / 塞最后」
>
> 这是 v5.1 修掉的一个真 bug。早期实现让插段二选一：塞在正文第一个字之前，或者追加到末尾。
>
> 问题在于**节点正文往往以转场句开头**：
>
> ```
> 你出了靖安司，没有直接去查案。     ← 转场句
>
> 你绕进了一条暗巷——平康坊的地下暗市。
>
> 葛老……坐在一堆绢帛中间，正在喝茶。
> ```
>
> 插段一律塞在最前，就成了：
>
> ```
> 你还没去坊里，葛老却像早算到你会路过这儿。   ← 插段
> 「你要找的那姑娘，」他说……
>
> 你出了靖安司，没有直接去查案。              ← 正文转场句
> 你绕进了一条暗巷——平康坊的地下暗市。
> ```
>
> **先提到葛老，再说你走进葛老的暗市**——预告式倒叙。玩家读到的是「我说了话，但故事没接住」。
>
> 标记把插入点交给**正文结构**决定，而不是交给作者猜。

**两条硬规则**（`tools/verify.mjs` 与 `tools/simulate.mjs` 会盯着）：

1. **插段要写成"承接"，不能写成"预告"。** 它落在标记之后，所以读起来必须接着上一句往下走。
   - 反例：`你还没去坊里，葛老却像早算到你会路过这儿。`（标记在"你绕进了暗市"之后 → 自相矛盾）
   - 正例：`你没提闻染，葛老却像是早算到你会来问这个。`
2. **数组顺序 = 读者看到的顺序。** 多条插段是**同时生效**（不是多选一），所以按时间顺序排。
   典型事故：无条件插段写「十七桶」，条件插段写「只剩九只」——两条一起插入就打架了。

**② `textVariants`（备选）** —— 整段重写，取第一个匹配者。只在「整场戏基调都变了」时使用，因为它要复制全文，容易与默认正文失同步。

两者可叠加：先选变体，再把插段套上去。

### 这些机制的验收标准

一条选择是否"真的留了痕迹"，不是靠感觉，而是可测的：

- `npm run verify` —— 条件引用的 flag 必须真的有人设置（防「幽灵条件」）；每个节点必须至少留一个**无条件**选项（防死锁）；条件正文的 `when` 键名与引用合法；有插段的节点必须写 `{{inserts}}` 标记（漏写会退化成"追加到末尾"，会报警告）。
- `npm run audit:branch` —— 统计**死标记**（写了但没人读的 flag）与**无痕迹节点**（走向相同且所设 flag 无人消费）。目标是保持 0。
- `npm run test`（内含 `simulate`）—— 随机 3000 局的**插段叠加审计**：枚举"哪些插段会一起出现"。文字层面的矛盾（「十七桶」vs「只剩九只」）静态检查抓不到，只能这样枚举出来看。当前全剧最多只有 2 条同时生效。
- `npm run smoke:text` —— 用真实浏览器跑「总选第一项」vs「总选最后项」两条走法，比对共同节点的**渲染文本**。当前 19 个共同节点里 18 个正文不同（唯一相同的是 `start`，第一个节点前面没有来路）。
- `npm run dump -- last` —— 把一局完整流程按**玩家视角**打印成可读文本，用肉眼从头读到尾。脚本测不出"读起来通不通"，只能看。支持 `first` / `last` / `random 7` / `seq 1,1,2,1`（按序号走指定支路）。
- `node tools/trace_choices.mjs` —— **逐选项追问「选完之后紧接着读到什么」**。每个选项打印：写到哪些 flag → 目标节点 → **该选项真正命中的那段插段** → 后文有没有读这个 flag。这是唯一能把"我的选择被后文接住了吗"摊开看的视图；`--brief` 只看摘要，加节点名只看单个节点。
  （完整结果可以导出留档：`npm run trace > reports/choice_trace.txt` —— v5.3 就是靠通读这份 800 行对照表找出那 3 处"接不住"的）

> ⚠️ **重要**：被 `require` 挡住的选项**不参与人格计分上限的计算**。`nodeMax` / `mbtiNodeStats` 都按「当前实际可选的下标集合」归一化，否则选项一少，达成率就会虚高。

---

## 人格测评：MBTI

### 4 个维度（映射到长安语境）

| 维度 | 左 / 右 | 长安语境 |
|---|---|---|
| **E / I** | 外向 / 内向 | 调兵遣将广结人脉 ↔ 独自追查深思独行 |
| **S / N** | 实感 / 直觉 | 查户籍看脚印重物证 ↔ 推断动机看朝堂大局 |
| **T / F** | 思考 / 情感 | 就事论事冷酷决断 ↔ 顾及人情宽恕共情 |
| **J / P** | 判断 / 知觉 | 守计划执行到底 ↔ 见机行事随机应变 |

- 每个抉择在 4 个维度各有 **-3 ~ +3** 的倾向分（正值偏 E/S/T/J，负值偏 I/N/F/P），共 66 个选项权重全覆盖。
- 结局页按**偏好强度**显示双向百分比条，如 `E 68% / I 32%`。
- 每维取较高一侧，组合成 **4 字母类型**（如 `ENTJ`）。

### 计分模型：机会基线归一化

朴素做法（「得分数 ÷ 满分」）在这个剧情树里会**系统性失真**：不同分支路过的节点数不同、可选选项数不同，某些维度在某些分支上根本拿不到负分，结果就是所有人都被推向同一侧 —— 旧版实测随机乱选会得到 **E 80.5% / I 19.5%、T 16% / F 84%** 这种严重偏斜。

v4 改为**机会基线归一化**：先统计每个维度在本局**实际经过**的节点上「可达到的均值 / 上限 / 下限」，再看你的得分相对均值往哪边偏了多少：

```
dev     = 得分 - 均值
upRoom  = 上限 - 均值      # 往上还有多少空间
downRoom= 均值 - 下限      # 往下还有多少空间

dev > 0 且 upRoom  > 0  →  左侧占比 = 50 + 50 × min(1, dev / upRoom)
dev < 0 且 downRoom > 0  →  左侧占比 = 50 - 50 × min(1, -dev / downRoom)
上下都没有空间（spread ≈ 0）→  该维度无法判定，标为「无偏好」
```

这样 **50% 线严格等于「和机会均值的偏差为零」**，左右两侧天然等概率。

### 平局怎么处理

旧版存在一个自相矛盾：进度条显示 50/50，字母却硬判给右侧。v4 显式区分三种状态：

- **有偏好**：正常给出字母与百分比
- **无偏好（tie）**：偏差恰好为 0 → 标为「无偏好」，并在结局页注明
- **无法判定（none）**：该维度全区间无可达空间 → 同样注明

实测 3000 局随机：**5.4% 的对局至少有一个维度落在 50/50**，且都会被如实标注。

### 分布实测

**3000 局随机**（`npm run simulate`）：

```
E/I: E=51.6%  I=48.4%   (无偏好 2.8%)
S/N: S=50.1%  N=49.9%   (无偏好 0.0%)
T/F: T=50.8%  F=49.2%   (无偏好 0.0%)
J/P: J=49.5%  P=50.5%   (无偏好 2.7%)
16 / 16 型全部可达
```

**24 局真实浏览器**（`npm run dist`）：

```
E/I: E=16  I= 8  |  S/N: S=11  N=13
T/F: T=12  F=12  |  J/P: J=10  P=14
出现 11 种类型，8 个字母全部可达
```

> 四维都已收敛在 47–53%，不再有系统性偏向。

### 16 型 ↔ 16 位书中人物（一一对应）

| 类型 | 人物 | 称号 | | 类型 | 人物 | 称号 |
|---|---|---|---|---|---|---|
| INTJ | 李必 | 执棋者 | | ISTJ | 崔器 | 守序的军人 |
| INTP | 徐宾 | 案牍中的推演者 | | ISFJ | 檀棋 | 默默护持者 |
| ENTJ | 林九郎 | 权柄执掌者 | | ESTJ | 元载 | 实务钻营者 |
| ENTP | 姚汝能 | 冷眼旁观者 | | ESFJ | 长安百姓 | 邻里长者 |
| INFJ | 何执正 | 心怀天下者 | | ISTP | 张小敬 | 刀锋上的实干者 |
| INFP | 丁瞳儿 | 守灯人 | | ISFP | 闻染 | 随性而行者 |
| ENFJ | 王韫秀 | 聚火者 | | ESTP | 龙波 | 孤注一掷者 |
| ENFP | 鱼肠 | 暗处的刃 | | ESFP | 葛老 | 坊间活字典 |

### 匹配逻辑（三层）

1. **主匹配**：与你 MBTI 型相同的人物（16 型 ↔ 16 人物确定性对应，零重复）
2. **契合度**：你的四维强度与该人物向量的平均偏差 → `契合度 76%`
   - 高 = 你是这个型的「典型样本」；偏低 = 你处在这个型的边缘
3. **次席**：比较「特质形状」给出第二接近的人物，作为补充视角

### 长安六维（辅助画像）

结局页另附 **6 维长安人格**（义气 / 谋略 / 仁恕 / 胆魄 / 信义 / 执念），用于剧情化解读与「最相似角色」，与 MBTI 互为补充，不是独立测评。

---

## ⚠️ 这个测评科学吗？——如实说明

**结论：作为心理测量工具，它不科学，请不要当作人格测验使用。**

上面的统计修复（机会基线归一化、平局标注）只解决了**计分是否自洽**，没有、也不可能解决**测量是否有效**。这是两件事，容易被混淆，所以下面分开讲。

### 它不成立的地方（按严重程度）

| # | 问题 | 说明 |
|---|---|---|
| 1 | **构念效度缺失** | 题目是「剧情里的决策」，不是「关于你自己的陈述」。没有任何证据表明「在唐代危机里会不会拔刀」能预测现实中的外向性。没有效标关联效度，没有与任何成熟量表（BFI-2 / NEO-PI-R）的聚合效度检验。**这是最根本的缺陷，其余问题都在其次。** |
| 2 | **权重是作者按「叙事合理性」手工指定的** | 没有因子分析、没有项目校准、没有专家评定、没有 IRT。所以「拔刀 = +2 E」只是一个文学判断，不是测量参数。我们因此无法知道这 66 道题究竟测到了几个维度。 |
| 3 | **没有常模样本** | 结果里的 `E 68%` **不是百分位**。它只是相对「本局机会基线」的线性重标定，没有人群参照。所以不能说「你比 68% 的人更外向」，只能说「你在这局里的作答偏离随机基线多少」。跨人比较在统计上是无意义的。 |
| 4 | **人物「契合度 %」是循环论证** | 我们先给 16 位角色手工编写了「人格向量」，再去衡量你和这些向量的距离。参照系是我们自己编的，因此这个百分比没有任何外部依据。 |
| 5 | **把连续变量切成了类型** | 这是 MBTI 本身的原罪，我们也继承了。详见下节。 |

### 我们实测到的统计性质（有好有坏）

用 `npm run audit` 可复现下面每一个数字：

**✅ 做得还行的地方**

> 以下数字由 `npm run audit` 实测得出。它是蒙特卡洛抽样，同一份代码每次运行会有小数点后的浮动。

- **分布形状是对的**：结果百分比呈单峰钟形，中心在 50，标准差 12.5~13.6，**极端值（0% / 100%）出现率 0.0%**——符合「多数人的特质在中间」这一真实特征，没有出现把所有人推向两极的人造现象。
- **4 个维度基本正交**：跨维度最大相关仅 **r = 0.10**（EI~JP），远低于 0.3 的独立判据。说明这 4 个维度在权重层面确实是可分的，没有退化成一两个维度。
- **对单题扰动稳定**：只改 1 个回答时，4 字母类型一致率 **约 93%~95%**，单维度一致率约 98%（蒙特卡洛估计，每次运行略有浮动）。

**❌ 站不住的地方**

- **S/N 维度题量不足**：66 道题里只有 **20 道**在 S/N 上有信号（E/I 有 35 道）。该维度的测量精度明显低于其他三维，值得后续补题。
- **路径依赖污染基线**：因为不同分支经过的题目不同，**「机会基线」本身会随路径漂移**。换算成测量噪声：
  - E/I ≈ 1.6 个百分点，S/N ≈ 2.4，T/F ≈ 0.6，**J/P ≈ 3.1**
  - 对照量表本身的标准差约 13.0，J/P 这条噪声占了约 **25%**。
  - 正规模表里每个人做的是同一套题，这条噪声应当为 **0**。这是本设计特有的结构缺陷。
- **超过一半的题是「复合题」**：**55%**（36/66）的选项会同时改动 ≥2 个维度，单题最多同时改 3 个。测量学上这叫 double-barreled item——无法归因到底是哪一个维度在被测量。
- **约 57% 的维度落在脆弱区**：落在 40%~60% 这个「差一两个回答就会翻面」区间的维度占 **约 57%**。加上我们的标准差（12.5~13.6）比真实量表（15~20）更窄，说明分数被压缩在中间，字母判定的边界稳定性偏低。

### 关于「类型」这件事本身

真实的人格特质在人群中是**连续且单峰**的，大多数人落在中间，而不是分成两堆。所以「你是 E 还是 I」这种二分，本质上是**对连续变量做中位数切分**的产物——切点附近的人，换几个答案就会换类型。

这也正是经典 MBTI 最大的实证问题：

- **重测信度差**：4~5 周后重测，约 **50%** 的人会拿到完全不同的 4 字母结果（Pittenger 1993，后续多项研究复现）；单个维度一致率约 65%~85%。
- 对照：**大五人格**（NEO-PI-R / BFI-2）的重测相关 r ≈ **0.70~0.85**，且维度是连续的、有效度证据充分。

我们的「隔一阵子再测」模拟（让被试的潜在倾向小幅漂移）结果（`npm run audit` 实测，蒙特卡洛抽样，每次运行有 ±2 个百分点左右的浮动）：

| 倾向漂移 σ | 类型一致率 | 单维度一致率 |
|---|---|---|
| 0.10 | ≈89% | ≈97% |
| 0.20 | ≈80% | ≈94% |
| 0.30 | ≈72% | ≈92% |
| 0.50 | ≈56% | ≈87% |

σ 表示「两次作答之间这个人变了多少」。可以看出：**一旦人自身有小幅变化（σ=0.5），类型一致率就掉到 56% 左右**，与真实 MBTI 的重测表现量级相当。二分法本身就在放大不稳定性。

### 那它有什么用？

有，但不是「测评」：

- **作为游戏机制**，它是称职的：它让 66 个选择汇成一个个性化的收尾，并与结局门控联动，这是设计上的成功。
- **作为自我觉察的引子**，可以——前提是把它读成「你这局是怎么行动的」，而不是「你是什么样的人」。
- **作为教学样例**，很适合：它现场演示了「把连续特质二分」会带来什么后果。

### 想真正做测量该怎么做

1. 换成**连续计分**，报告维度分与标准误，不报类型码。
2. 建**常模样本**（哪怕几百人），把原始分转成百分位。
3. 做 **EFA / CFA**，用数据而非直觉确定维度数与题目归属；删掉 double-barreled 题。
4. 与 **BFI-2 / IPIP-NEO** 做聚合与区分效度检验。
5. 改测**大五**而非 MBTI——构念本身效度更好，且已有大量公开题库（IPIP 属公有领域）。
6. 若要临床或科研使用，请用**经过验证的工具**（NEO-PI-R、BFI-2、PID-5 等），本工具不具备任何诊断资格。

---

## 文件结构

```
长安十二/
├── index.html                  入口 + 顶栏（十二时辰刻度）+ 侧栏 + 结局浮层
├── results.html                结局图鉴页（从 scenes.js 反向推导，无硬编码文案）
├── styles.css                  唐代暗色战报风 + 响应式抽屉 + 无障碍样式
├── start.bat                   一键启动（检查 Python → 起服务 → 开浏览器）
├── package.json                npm scripts 快捷入口
├── docs/
│   └── CANON.md                原著设定基准 + 逐项偏离清单（人工核对，不可自动化）
├── src/
│   ├── main.js                 剧情引擎 + 条件系统 + 存档 + 人格/MBTI 计分 + 渲染
│   ├── text.js                 条件正文解析（引擎与 results.html 共用，唯一的实现）
│   └── data/
│       ├── characters.js       16 位人物
│       ├── scenes.js           22 个抉择节点 + 59 个过场场景 + 4 个结局（共 85 节点）
│       │                       + 66 个抉择（含条件门控）+ isPassage / nextTargets 两个图遍历入口
│       ├── traits.js           长安六维体系 + 66 抉择权重 + 16 角色向量
│       └── mbti.js             MBTI 4 维 + 66 抉择权重 + 16 型定义 + 16 角色向量
└── tools/                      校验与测试（不参与运行时）
    ├── verify.mjs              Node：数据契约全覆盖校验 + 跨结局一致性 + pov 一致性 + 成对符号配对
    ├── simulate.mjs            Node：3000 局随机模拟回归 + 插段叠加审计（免浏览器）
    ├── psy_audit.mjs           Node：心理测量学审查（题量/基线/分布/稳定性/结构效度）
    ├── branch_audit.mjs        Node：分支拓扑审查（真分叉 / 有痕迹 / 无痕迹 + 死标记）
    ├── passage_audit.mjs       Node：过场场景是否在复述上游/汇合点的正文与插段
    ├── continuity_audit.mjs    Node：转场连续性（换场 / 跨时辰 / 换视角 / 时辰自称不符）
    ├── trace_choices.mjs       Node：逐选项追踪「选完之后紧接着读到什么」（含三值条件预览）
    ├── _browser.py             Python：浏览器探测 / 代理绕行 / 存档清理（被下面共用）
    ├── dump_play.py            Python：按玩家视角打印一局流程 / 一次读完 4 个结局（人工审阅）
    ├── smoke.py                浏览器冒烟（基本流程 + 控制台无错）
    ├── smoke_psy.py            长安六维差异性（三策略 → 三画像）
    ├── smoke_mbti.py           MBTI 差异性（三策略 → 三类型）
    ├── smoke_text.py           条件正文验证（两条走法比对共同节点的渲染文本）
    ├── smoke_gate.py           结局门控集成测试（驱动同路人高/低两种打法）
    ├── smoke_results.py        results.html 图鉴页渲染冒烟（断言卡片真的渲染出来）
    ├── shot_passage.py         过场场景的视觉抽查（截图 + 点「继续」确认真的跳转）
    ├── mbti_dist.py            真实浏览器 N 局统计 MBTI 分布
    ├── migrate_inserts.py      一次性迁移脚本：pos → 正文 {{inserts}} 标记（已执行完，可删）
    └── requirements.txt        Python 测试依赖（仅 playwright）
```

### 架构约定

- **数据与引擎分离**：`src/data/*.js` 是纯数据模块（无副作用、无 DOM 依赖），`src/main.js` 是引擎。数据模块可被 Node 测试脚本直接 `import`，因此 `verify.mjs` / `simulate.mjs` 不需要浏览器就能跑。
- **零运行时依赖**：`package.json` 里的 `playwright` 相关只服务于测试，`src/` 下不 import 任何第三方包。
- **改选项顺序必须同步改权重**：`scenes.js` 里某个节点的选项增删或换序后，`traits.js` 的 `CHOICE_TRAITS` 与 `mbti.js` 的对应权重表必须同步改下标，否则 `verify` 会报错。这是刻意设计的 —— 让错位在 CI 里立刻暴露，而不是静默算错分。

---

## 校验与测试

```bash
# 数据契约全覆盖校验（85 节点 / 66 抉择 / 4 结局 / 权重覆盖 / 16 型 / 16 角色）
npm run verify

# 3000 局随机模拟回归 + 插段叠加审计 —— 免浏览器，秒级
npm run simulate

# 心理测量学审查（题量覆盖 / 基线一致性 / 分布形状 / 稳定性 / 结构效度）
npm run audit

# 剧情分叉审计（死标记 / 无痕迹节点 / 真分叉 / 不可绕过的主干）
npm run audit:branch

# 过场场景重复检查（59 段过场 vs 上游抉择点 / 汇合点 / 两处插段）
npm run audit:passage
node tools/passage_audit.mjs digest   # 打印紧凑清单，逐对人工通读

# 转场连续性审计（每一次换场 + 跨时辰 + 换视角 + 正文自称时辰与 node.hour 是否一致）
npm run audit:continuity

# 逐选项追踪「选完之后紧接着读到什么」（含"你的选择真正命中的插段"）
npm run trace:brief      # 摘要：每个选项 → 目标节点 → 会命中的插段
npm run trace            # 全文：再加上后文引用情况
npm run trace -- h_hai   # 只看某个节点

# 上面几项一起跑（推荐提交前跑这个）
npm test
npm run test:all        # 再加上分叉审计、过场审计与心理测量审查

# 以下需要先起本地服务（python -m http.server 8010）并装好 playwright
npm run dump -- last    # 把一局完整流程按玩家视角打印出来，人工读一遍
npm run dump -- seq 1,1,1,1,1,1,2,1   # 走指定支路（1-based，越界取最后一项）
npm run dump:endings    # 4 个结局各读两遍（同路人拉满 / 一个都没有）—— 改过主线正文必跑
npm run smoke           # 基本流程 + 控制台无错
npm run smoke:psy       # 六维差异性
npm run smoke:mbti      # MBTI 差异性
npm run smoke:text      # 条件正文：两条走法比对共同节点的渲染文本
npm run smoke:gate      # 结局门控集成测试
npm run smoke:results   # results.html 图鉴页渲染（卡片数 + 无标记泄漏 + 无运行时错误）
npm run shot:passage    # 过场场景视觉抽查（截图 tools/passage_shot.png + 点「继续」确认跳转）
npm run dist            # 24 局真实浏览器 MBTI 分布
```

### `verify` 覆盖项目

```
节点总数: 85        可达节点: 85      结局数: 4
覆盖时辰: 12 / 12   选项总数: 66（其中条件选项 5，条件路由 1）
过场场景: 59 个（只有 next、没有 choices —— 不算抉择、不计分）
条件正文: 插段 78 条（20 节点，其中 20 个带 {{inserts}} 插入点）· 整段重写 1 条（1 节点）
可设置 flag: 65（被条件引用 62）    同路人门槛: 信任 >= 75
人格权重覆盖: 66 / 66                MBTI 权重覆盖: 66 / 66
MBTI 16 型定义: 16 / 16              角色人格向量: 16 / 16
角色 MBTI 向量: 16 / 16
```

它会检查：BFS 连通性（无孤儿节点）、条件表达式引用的 flag 是否真的存在（防「幽灵 flag」）、每个节点是否至少有一个**无条件**可选项（防死锁）、`next` 指向的节点是否存在、过场场景有没有写 `nextLabel`、权重表长度与选项数是否一致、有插段的节点是否写了 `{{inserts}}` 标记（漏写会退化成"追加到末尾"）、无条件插段是否会跟条件插段叠加、`pov` 是否真的是叙述者、16 型与角色向量是否齐全、**「谁在场」跨结局一致性**、**门控可达性**、**成对符号 `「」` `“”` `（）` `《》` 是否配对**（少一个引号浏览器照样渲染，只会静默上线），以及**支路饱满度**（见下）。

### 人工审阅为什么不能省

`verify` / `simulate` 只能证明**结构**没错——节点连通、flag 有人读、结局可达、权重覆盖。它们证明不了**读起来通不通**。

v5.1 修掉的那个「预告式倒叙」就是结构全绿但没法读的典型：节点连通、flag 都被消费、测试全过，但玩家读到的是「先提到葛老、再说你走进葛老的暗市」。

v5.2 又抓到一类**只有在整条线读完才看得见**的毛病：

| 症状 | 为什么自动化检查抓不到 |
|---|---|
| `h_hai` 正文说「他们全都上来了」，可达的 `e_burn_fire` 却说「你身后没有人」 | 单看任一段都通顺，矛盾跨节点 |
| 结局写「你想起闻染说的话——长安不用你守」，但**这句台词全剧从未出现** | 要在全库范围检索才知道它只出现这一次 |
| `h_wei` 标 `pov: 'libi'`，侧栏显示「李必视角」，正文却是「李必从屏风后走出来」 | 元数据与正文的一致性，跨字段 |
| `h_shen` 正文写「伤口在流血」，但可达的 `observe` 支路里你根本没动手 | 同上，跨节点 |

所以 `tools/dump_play.py` 是必需的：它把真实浏览器渲染出来的每一段按顺序打印，像读者一样从头读到尾。

v5.4 又抓到第四类：**过场在复述。** 这一轮给 21 个抉择点的每个选项都加了一段专属过场，于是出现了新的失败模式——过场把上一场或下一场的话又念了一遍。

| 症状 | 为什么自动化检查抓不到 |
|---|---|
| `x_task_lone` 结尾写「身后没有人。」，汇合点 `h_zi2_gela` 的插段写「你身后一个人也没有。」 | **换了说法**的重复，字面最长公共子串只有 5 字 |
| `x_wu_doc` 让崔器把「边军冬衣」念第三遍（基座正文念过、玩家选项里念过） | 三次分散在三个字段里，要连起来读才发现 |
| `h_wu` 的选项 4 说「调拨单上写的是『边军冬衣』」——可 `h_wu` 的基座正文里崔器**自己**已经说了同一句 | 选项文案与基座正文的矛盾 |
| `x_gela_power` 选了「亮靖安司腰牌」，过场却从「你办完事」写起，**把玩家刚点的那个动作整个跳过去了** | 结构上完全正常，只有读到才发现"我点的那一下没演" |
| `x_tail_help` 写「你身上没有腰牌，也没有官凭」，`x_doc_confront` 写「把靖安司的令牌亮出来」 | 两条路径在同一条时间线上，跨节点对立 |

为此新写了 `tools/passage_audit.mjs`（`npm run audit:passage`）：把每段过场与**三个对象**逐一比对——上游抉择点、汇合点、两处的条件插段。度量用**最长公共子串**（抓照抄）+ **三元组重合系数**（抓"换了个字"的重复，例如「文书你先送回**了**」vs「文书你先送**去**了」）。

> 这个工具本身也是被通读逼出来的：第一版只比下游汇合点，结果漏掉了 `x_doc_confront` 照抄上游 `h_si_doc` 整整 34 个字——**过场最容易复述的不是下一场戏，是上一场戏**。

**改完正文必须跑这几条：**

```bash
npm run dump -- last        # 从头读到尾，看有没有接不住的句子
npm run dump:endings        # 4 个结局各读两遍（同路人拉满 / 一个都没有）
npm run audit:passage       # 过场有没有在复述上游 / 汇合点 / 插段
node tools/passage_audit.mjs digest > reports/passage_digest.txt   # 逐对人工通读
```

`endings` 模式之所以必要：4 个结局是同一个分流点（`h_hai`）的四个出口，而 `h_hai` 之前的 22 个节点**全部**能走到这 4 个结局。也就是说主线是共享的——**改任何一段主线正文，都得回来把这 4 段并排读一遍**，确认没有哪一句被某个结局当面否掉。该模式还会断言渲染结果里没有 `{{inserts}}` 标记残留（主引擎曾经直接读 `sc.text`，导致结局的插段不渲染、标记原样显示给玩家）。

### Python 测试环境

只需 `playwright`：

```bash
pip install -r tools/requirements.txt
```

**不需要** `playwright install` —— `tools/_browser.py` 会自动探测本机已装的 Chrome / Edge（也支持 `CHANGAN_BROWSER` 环境变量指定），并复用系统浏览器。探测失败时会给出明确的中文报错，而不是抛一个看不懂的堆栈。

---

## 存档

- 进度自动写入 `localStorage`（键名 `changan-save-v1`），刷新页面后可续玩，页面上会显示「继续上次进度」提示。
- 隐私模式下 `localStorage` 不可用时**静默降级**，不影响正常游玩。
- 顶栏「重来」按钮会二次确认后清空存档。

---

## 交互与无障碍

- **键盘**：`1` `2` `3` 直接选择对应选项；结局浮层上按 `Enter` / `空格` 关闭。
- **焦点**：所有可交互元素都有 `:focus-visible` 样式；结局浮层打开时会把焦点移到关闭按钮。
- **读屏**：结局浮层带 `role="dialog"` 与 `aria-hidden` 状态切换。
- **移动端**：侧栏在小屏改为**抽屉式**侧滑面板（`transform: translateX`），不再用 `display: none` 直接隐藏 —— 否则手机上会看不到「同路人」，而结局正是按同路人判定的。顶栏有开关按钮。
- **动效**：尊重 `prefers-reduced-motion`，关闭过渡动画。
- **字体栈**：Windows 优先（思源宋体 / 微软雅黑 等），带跨平台回退。
- **图标降级**：CDN 图标加载失败时自动加 `.no-icons` 类，走纯文字排版，不会出现空白方块。

### 调试钩子

URL 加 `?debug=1` 会暴露内部状态，供集成测试按「结论」驱动剧情：

```js
window.__changan = { state, countAllies, evalCond, resolveTo, scenes, characters, ALLY_TRUST,
                     goto(id),      // 跳到某个节点/结局并渲染
                     setTrust(obj) } // 直接设定信任度，用来构造极端局面
```

例如 `smoke_gate.py` 用它算出「本局怎么选才能把同路人拉到 5」，再照此走完，从而稳定地分别触发不同结局。

`goto` / `setTrust` 是给 `npm run dump:endings` 用的：结局只有走到才能看见，而它恰恰是条件插段最密的地方（`e_final_people` 有 5 条），手工复现成本太高。

---

## 注意事项

- **这不是心理测验。** 它是一个游戏机制，恰好长得像 MBTI。请勿据此对任何人（包括你自己）下判断，更**不构成心理诊断**。详见上文「这个测评科学吗」。
- 百分比是相对「本局机会基线」的重标定值，**不是百分位**，不能跨人比较。
- 达成率基于你**实际经过**的节点计算，因此同一局内各维度之间可比较。

---

## 已知限制 / 待办

**剧情结构（`npm run audit:branch`）—— 已大幅改善，但还不是"真分支"**

实测数据（修复前 → 修复后）：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 选择完全没后果的节点 | 20 / 22（91%） | **0 / 22（0%）** |
| flag 只写不读（死标记） | 62 / 64（97%） | **0 / 66（0%）** |
| 选项总数 | 64 | 66 |
| 条件选项 / 条件路由 | 0 / 0 | 5 / 1 |
| 条件正文（插段 / 整段重写） | 0 | 73 / 1 |
| 两条走法在共同节点的正文差异 | 0 / 19 | **18 / 19** |
| 插段落在**结构正确**的位置（不产生倒叙） | 0 / 17 节点 | **19 / 19 节点** |
| 同节点最多同时生效的插段数 | — | 5（`e_final_people` 按同路人逐个点名，已显式声明） |
| 侧栏「当前视角」与叙述者不符的节点 | 6 / 26 | **0 / 26** |
| 门槛卡在两个可达值中间（提示容易指向走不通的路） | — | **0**（v5.3 起 `verify` 会报警） |
| 一个出口有多条来路、开场却只写了一条 | 1（`e_final_persuade`） | **0** |
| **每个选项的去处互不相同的抉择点** | 2 / 22 | **21 / 22**（v5.4） |
| **其中"先走专属过场、再汇合"的** | 0 | **20 / 22**（v5.4） |
| **过场复述上游 / 汇合点正文（> 8 字）** | — | **0**（v5.4 起 `npm run audit:passage` 会报错） |
| 成对符号 `「」` 不配对的节点 | 未检查 | **0**（v5.4 起 `verify` 会报错） |

- [x] ~~伪选择：选什么都一样~~ → 每个选项的 flag 现在都会被后续读到，会改变你读到的文字
- [x] ~~62 个死 flag~~ → 已全部接上（4 个终局标记除外，它们在结局分支上，按定义无处可读）
- [x] ~~侧栏「当前视角」显示错误的角色~~ → v5.2 修掉 6 个节点错标的 `pov`
      （`s1_meet` / `s1_task` / `h_chou` / `h_chen` / `h_wu` / `h_wei`），
      并加了自动判据：*pov 角色的名字出现在正文里 ⇒ 叙述者不可能是他*。
- [x] ~~「选了话但故事没接住」~~ → v5.1 把插段定位从「塞最前/塞最后」改成正文里的 `{{inserts}}` 标记，
      并逐节点重写那些写成"预告"而不是"承接"的插段。详见上文「为什么不用塞最前/塞最后」。
- [x] ~~建议方向：把 `h_mao` 已验证的「支路—汇合」模式复制到主干节点~~ → **v5.4 已铺开**：
      21 个抉择点各有互不相同的去处，20 个走「专属过场 → 汇合」，共 59 段过场。
- [ ] **仍然只有 2 个「真分支」节点**：`h_mao`（西市追查，三选一，三条支路各自还有自己的选择题）
      和 `h_hai`（灯楼对峙，四选一）。其余 20 个抉择点现在是「过场—汇合」——
      **你的选择真的把你带到了一段别处，但走完还是会回到同一条线上。**
      这是"绕行"的升级版，不是分叉；要变成真分支，得让某条支路**不再汇回**。
- [ ] **59 段过场目前都只有一个出口**，是"专属戏"而不是"岔路"。
      下一步若要真分支，只需挑 2~3 段让它们各自接到不同节点（过场本来就支持，改 `next` 即可）。
- [ ] 不可绕过的主干是 **19 个抉择节点** + 4 个结局（v5.4 起由 `branch_audit` 从数据算出，
      不再写死"7 个节点"）；可绕过的场景 **62 个**（3 抉择点 + 59 过场）

**测量层面（`npm run audit` 实测得出）**

- [ ] **S/N 维度题量不足**：仅 20/66 道题带信号（E/I 有 35 道），需补题提升精度
- [ ] **路径依赖污染基线**：J/P 维度约 3.1 个百分点的基线噪声（占量表 SD 的 ~25%），正规模表应为 0
- [ ] **55% 是 double-barreled 题**（同时改 ≥2 个维度），需拆分或重新归因
- [ ] 无常模样本 → 百分比无法解释为百分位
- [ ] 未做 EFA / CFA，维度归属从未被数据检验
- [ ] 人物「契合度 %」为循环论证，应删除或明确标注为叙事装饰

**工程层面**

- [ ] 尚无 `LICENSE` 文件（当前仅在 README 声明保留权利）
- [ ] 多视角切换（除张小敬外，可在李必 / 崔器视角下分支）
- [ ] 角色台词音效（古琴 / 更鼓 / 钟声）
- [ ] 上元灯会场景的 SVG 灯笼可视化
- [ ] 鱼肠支线 / 林九郎支线（隐藏分支）
- [ ] 人格结果可分享（生成卡片图）

---

## 改动日志

### v6.0 (2026-09-11) — 用户拍板：修正所有偏离，逐处择优（原著/剧版）

用户原话：**「要求修正所有偏离，对于到底按原著还是电视剧，择优」** —— 即路线 A（全面对齐），且每处对小说与剧版取更优者。

#### 一、逐处择优基线（已写入 CANON.md 第 0 节）

| 偏离点 | 原著（小说） | 剧版（2019） | 本作取舍 |
|---|---|---|---|
| 鱼肠性别 | 男 | 女 | 剧版·女 |
| 张小敬左眼 | 瞎 | 不瞎 | 剧版·不瞎 |
| 烽燧堡幸存 | 3 人（闻无忌/萧规/张小敬） | 8~9 人 | 两说并存（写"仅余数人"），核心三人必保留以支撑相认 |
| 闻染结局 | — | 跳井（被鱼肠逼） | 剧版作背景注脚 |
| 龙波＝萧规 / 第八团 / 入狱原因 / 灯楼兴庆宫 / 狼卫vs蚍蜉 / 阙勒霍多 | 同 | 同 | 两版一致→直接统一补全 |
| 王韫秀/王蕴秀 | 史：王韫秀+王忠嗣 | 剧：王蕴秀+王宗汜 | 剧版全一致→王蕴秀+王宗汜 |

#### 二、代码层修正（src/data/characters.js + scenes.js）

- **龙波**：`alias` 狼卫首领→蚍蜉之主·本名萧规；`bio` 补第八团护旗手/烽燧堡/改名动机；`side` 狼卫→蚍蜉。
- **张小敬**：`bio` 补闻无忌/熊火帮/永王/杀 34 人/斩县尉谭同寿/劫持永王/十恶·不义判斩；`h_yin` 补答"为何进死牢"。
- **闻染**：`alias`+`bio` 补闻无忌之女/闻记香铺/第八团烈士骨灰/恨长安/被龙波利用/爱慕张小敬；`h_si` 台词补立场。
- **鱼肠**：`alias` 神秘女子→龙波麾下刺客；`bio` 补倾心龙波/逼闻染投井；`h_you` 删"主事人＝长安城"误标，降回刺客。
- **灯楼位置**：`修政坊` 全替换为 `兴庆宫`（22 处）；`h_shen`/`h_hai` 补花萼相辉楼/圣人。
- **x_xu_hear**：替换 v5.4 编造的"陇右 137 人"独白为真实第八团/烽燧堡/萧规＋张小敬同袍＋改名龙波（CANON 1.3 自揭的错）。
- **h_hai**：加张小敬认出龙波＝萧规（第八团）的相认段（全路径通用）。
- **狼卫/蚍蜉**：`x_xu_hear` 澄清突厥狼卫（右刹/曹破延/图格鲁）是弃子、龙波借势；加龙波替太子李亨铺路动机。
- **阙勒霍多**：补伏火雷＋延州石脂（石油）（2 处）。
- **望楼**：`h_wei_roof` 补靖安司旗语信号系统（姚汝能设计）；`姚汝能`/`何执正`/`葛老`/`王蕴秀` 补身份细节。
- **檀棋**：`h_chen` 加正戏（李必身后侍女，递茶退回）—— 解决 ⑥ 严格档唯一残留 ❌。

#### 三、工具层

- `continuity_audit.mjs` 第 ⑥ 节 `CHARACTER_ACK` 键名 `王韫秀→王蕴秀` 修正；`檀棋`/`姚汝能` 确认更新为已真正出场。

**回归结果**：`verify` ✅ · `check:readme` ✅ 14/14（插段仍 75）· `simulate` ✅ · `branch_audit` ✅ · `passage_audit` ✅ · `continuity_audit` ✅（⑥ 严格档 0，EXIT=0）· `dump:endings` ✅ 4 结局无残留。

> **一句话总结**：v5.5 时"符合设定"是红、且一行未动；v6.0 已按择优全面修正，自动化层与设定符合度层均收敛。

### v5.5 (2026-09-11) — 严格追问：「是不是符合书籍设定、十分连贯没有跳跃」

用户原话：**「我严格问你，现在呢，现在是不是在符合书籍设定的基础上，十分连贯不违和地写出了所有的情节，没有跳跃」**

诚实的答案分两半：

#### 一、「符合书籍设定」—— **不是，而且这一层我此前从未验证过**

前四轮（v5.1~v5.4）做的全是**内部自洽**：flag 有没有人读、两段文本有没有重复、时辰有没有跳。
**「符不符合原著」这一层，一次都没查过 —— 因为原著不在这份代码里。**

这次做了核对，结论是**系统性偏离**，且偏离集中在最有分量的地方。完整清单与来源见 **[`docs/CANON.md`](docs/CANON.md)**，README 顶部已加显眼声明。最重的五条：

| # | 偏离 | 级别 |
|---|---|---|
| 1 | **龙波与张小敬本应是第八团同袍（龙波＝萧规）**，游戏里素不相识；全篇无「第八团」「烽燧堡」「闻无忌」「萧规」 | **P0** |
| 2 | **张小敬入狱的真实原因**（杀熊火帮 34 人＋杀死上司万年县尉＋劫持永王）被抹成"为守护长安的百姓" | **P0** |
| 3 | **闻染本应极度恨长安、被龙波利用参与计划**，游戏里只是受害者／线索人（且她的立场台词被搬到了鱼肠嘴里） | **P0** |
| 4 | **龙波被标成「狼卫首领」** —— 他是「蚍蜉」首领；狼卫首领是右刹，且狼卫在计划里是弃子 | **P1** |
| 5 | **灯楼位置写"修政坊"** —— 原著/剧里大灯楼在兴庆宫，圣人在花萼相辉楼观灯 | **P1** |

> **其中第 6 条是我这一轮自己写出来的错**：v5.4 给龙波编了一句独白「陇右。十一年前，我是陇右的兵。我们那一营，一百三十七个。」
> 正好顶掉了原著最重的一块（第八团、烽燧堡、两百多人只剩几个），还把"张小敬自己也在第八团、两人应当在此相认"这个前提一起抹掉了。
> **编造设定比留下空白更糟 —— 空白只是缺失，编造会主动顶掉正确答案。**

#### 二、「没有跳跃」—— **时辰/视角干净，但"正文写死了部分来路才有的事"有 5 处，已全修**

新写 `tools/continuity_audit.mjs`（`npm run audit:continuity`），把 85 个节点、**127 条边**的每一次转场全摊出来：

```
换场（place 变化）83 条 · 跨时辰 0 条 · 换视角 0 条
① 时辰：每一步都落在相邻时辰，主线单调向前 ✅
② 视角：全程张小敬，从未跳换 ✅
```

**已修的 5 处硬矛盾：**

1. **`h_shen` 正文写死「李必在你旁边」** —— 但 `h_wei` 的 `decoy` 那条来路是"你把他们引开、司丞从后门走"，`observe` 那条你压根没动、司丞被人护走。
   这两条路上**李必是后来才找回你的**，正文写死 ⇒ 他会凭空出现在你旁边。
   → 按三条来路拆成三个互斥插段（`flagsAll: ['breakout']` / `['decoy']` / `['observe']`）。
   注意**不能**用 `flagsAny: ['breakout','decoy']` 合并：`breakout` 时它会同时命中两条，反而造出新的叠加矛盾。
   （修完又穷举了一遍：13,226,976 条到 `h_shen` 的来路，**每条都恰好命中 1 个「靖安司怎么出来」+ 1 个「望楼独处」flag**，所以正文那个"他"永远有指代对象。）
2. **`h_you_xubin` 正文开头写「夜半。」，而 `node.hour = 酉时`** —— 「夜半」是游戏自己 `hours` 表里**子时**的别名，差了整整两个时辰。
   → 改为「入夜。」。同时给 `continuity_audit` 加上**时辰别名检查**（原先只查"X时"这种写法，别名一律漏检）。
3. **`h_chou` 正文写「丑时。李必带你进了靖安司最深处的案牍库。」** —— 但这一线的上一幕你是**独自**出靖安司、独自进的暗市，三条过场也都是你一个人上台阶出来，李必从头到尾没出现。
   → 改为"你自己回来 + 他在廊下等"：「丑时。你回到靖安司。廊下只有一盏灯亮着。李必站在那里，没问你这两个时辰去了哪。」任何一条来路都成立。
4. **`h_chen` 正文写死「李必听完你的回报」** —— 而"报没报"由西市巷战那个选项决定。
   穷举 13,608 条到 `h_chen` 的来路：**11,664 条（86%）根本没报过**，这 86% 会读到一句凭空出现的汇报。
   → 正文里那句删掉，改成两条**互斥**插段：
   `flagsAll: ['reported']`（"西市的消息，你方才已经递回来了"）／`flagsNone: ['reported']`（"你把西市那一趟说了一遍"）。
   顺带修掉一处时间口径错误：原插段写「你**昨晚**就把消息报回了靖安司」，可西市在**卯时**、这场在**辰时**，同一清早隔两个时辰 —— 改成"方才"。
5. **`x_doc_avoid` 写"你没有往右骁卫去"，下一幕却是右骁卫军营** —— 军营在主干上、绕不掉，所以"绕过去"这个选项变成"说了不去、结果还是去了"，只能靠 `h_wu` 一条插段（"可有些人不绕过去不行"）硬拗，读者会觉得被拽。
   → 真正的语义是「**绕过去 = 先不摊开这份文书**」，不是"不见崔器"。过场改成"去军营之前先绕到茶棚想清楚"，人照去，只是不带文书上桌；`h_wu` 的 `avoid_cuiqi` 插段同步改成"文书还在你怀里，你决定先不摊开它"。
   → 这条**不是转场问题**，是 **病症 B（选项语义与走向不符）** —— 靠 ① 换场表看不出来，是 ⑤ 那节顺手抓到的。

> 前两处是"工具列出来了、人眼判断后修的"；第 3、4 处是靠**新加的第 ⑤ 节穷举**抓出来的 —— 详见下面。

#### 二·补 — 让"正文断言了不存在的事"变成可自动检测

上面这些矛盾的形状是同一个：**正文写死了一个只在部分来路上成立的事实**，而该节点**本来就有插段**在管这件事。
所以问题不是"没人接住"，而是"正文自己也断言了一遍"：插段变成废话，没插段的那条路变成矛盾。

`passage_audit` 只比"过场 ↔ 下游/上游"，**不比"本节点正文 vs 来路条件"** —— 这就是漏网的原因。
给 `continuity_audit` 加了第 ⑤ 节：

- 用状态可达性 `(node, hasFlag)` 在 **85 × 2 = 170 个状态**上做可达性（不是穷举 1,300 万条路径，但结论等价）。
- 对每个"有插段依赖它"的 flag 问：**到达该节点时它是不是必然的？**
- 非必然 ⇒ 「没有它」的那条来路必须读得通。再判这条来路有没有被接住：
  - 有 `flagsNone: [F]` 的插段 → 已接住
  - 或 F 的**互斥兄弟**（同一抉择点其它选项的 flag）各有插段 → 已接住（兄弟互斥，"没有 F"必然落到某个兄弟身上）
  - 都没有 → ⚠️ **请看**
- 当前结果：**67 组候选：已接住 63 · 已人工确认 4 · 请看 0**。

**关键设计：工具抓不全，所以它必须能被"人工确认"结案。**
工具没有能力判断"正文那句话算不算断言了 F 代表的事"，所以它只负责**把范围从"85 个节点全读一遍"缩到几条**。
剩下那几条必须有人通读、并把结论写进工具里的 `CONFIRMED_OK` 表（带理由）——
不写下来 = 下次跑还是红的；写下来 = 下一个人知道"这条被谁、按什么标准放过了"。
这不是绕过检查，是**把人的判断变成可追溯的记录**。

第 ⑤ 节还顺手抓到一处此前的漏网：
`h_si_doc` 的 `cold` 插段写「**你没打算看**。……推过来的东西，你不接，它就一直在那儿。」紧接着正文却是「**你翻开文书**」—— 前后顶住了。
→ 插段结尾改成「你盯了很久。最后还是伸手把它拿了过来。」，让"不接"到"翻开"之间有一个动作。

**反向验证（证明它真能抓住第 4 处那种 bug）**：把 `h_chen` 那条 `flagsNone: ['reported']` 的兜底插段临时改成永不成立，
工具立刻报出 `[h_chen] flag「reported」不是必然 —— 有的来路有它、有的没有。正文开头: 辰时。靖安司。…`，退出码 1；恢复后转绿。

#### 三、这一层为什么没有工具能代劳

`verify` / `simulate` / `branch_audit` / `passage_audit` / `continuity_audit` 检的全是**代码里已有的东西**。
它们**不可能**回答"龙波该不该认识张小敬"—— 原著不在这份代码里，任何自动检查都无法把自己的知识当标准答案。
所以设定符合度只能：**人工核对 → 写成表 → 逐项判定 → 人来拍板**。
**这不是工具不够好，是这类问题在原理上不可自动化。任何声称"全绿"的自动化报告都不覆盖这一层。**

#### 四、这一轮之后，"没有跳跃"还剩下什么（我没做，要你知道）

1. **檀棋只在结局出现一次**（⑥ 节报出来的）。已在 `e_final_people` 的插段里补了同位语「李必的侍女檀棋」，
   让那一句自己站得住；但**她仍然没有在正剧里出场过**。要真解决，得给她一场戏 —— 属于剧情设计，不是修 bug。
2. **人物表里 4 个"人格参照"从未进正文**（何执正 / 林九郎 / 丁瞳儿 / 王韫秀）。
   它们是结果页"你最像谁"的 MBTI 同型参照，设计上不出现在剧情里 —— 我判断这是可接受的，但**这是个判断，不是事实**，你可以推翻。
3. **§三 那一层（像不像原著）一条都没动** —— 因为你还没选路线 A / B / C。

#### 附带产出
- 新增 `tools/continuity_audit.mjs`（`npm run audit:continuity`：① 换场 ② 跨时辰 ③ 换视角 ④ 时辰推进表 ⑤ 基座正文的条件依赖 ⑥ 人物出场检查；
  共 6 节。**时辰自称不符 + ⑤ 的"未接住且未确认" + ⑥ 的"只在结局被点名且无人确认"会让 CI 失败**；换场表只列不判）
- 新增 `docs/CANON.md`：设定基准表（人物 22 项 / 事件 10 项，逐项带来源链接）＋ 分级偏离清单（P0~P3）＋ 三条可选路线
- `test:all` 纳入 `continuity_audit`
- 顺手修 `check_readme.mjs`：它要求 LF 换行，README 一旦被改存成 CRLF 就会误报"找不到 verify 输出块"（本轮真实踩到），现在读入时先归一化

**回归结果**：`verify` ✅ 0 错误 · `check:readme` ✅ 14/14 · `simulate` ✅ 4/4 结局可达 · `branch_audit` ✅ · `passage_audit` ✅ 0 处复述 · `continuity_audit` ✅ 0 处时辰矛盾 + ⑤ 请看 0 + ⑥ 严格档 1（已确认） · `psy_audit` ✅ · 6 个浏览器冒烟全过 · `dump:endings` ✅ 4 个结局无标记残留

> **一句话总结这一轮**：
> 我此前所有"全绿"的报告，证明的是**这份代码内部不打架**，从来没证明过**它像不像《长安十二时辰》**。
> 前者做得不错，后者**做得不好** —— 而且这件事必须由你选路线，我不该替你决定。

### v5.4 (2026-09-11) — 每个选项后面接一段只属于它的戏

起因：玩家追问「**那能不能不同的选项，都可以多一个场景，再把读者拉回来**」。

v5.3 证明了"接线是通的"，但通的结果是**所有选项最后都汇到同一段正文**——接住了，可是没有戏。这一轮把它补上：**给每个选项一段专属过场，然后再汇合回主干。**

- **新增节点类型：过场场景（passage）** —— 非结局、没有 `choices`、只有一个 `next` + `nextLabel`，引擎渲染成「继续」按钮。
  - 引擎（`src/main.js`）：新增 `advance()`，与 `choose()` 分开——**不碰 `choicesMade` / `traitRaw` / `mbtiRaw`**。`styles.css` 加 `.choices.passage` / `.continue-btn`（虚线 + 箭头 hover 平移）。
  - 数据（`src/data/scenes.js`）：**22 个抉择点里 21 个的全部选项改指过场，新增 59 个过场场景**。节点数 26 → **85**，而**选项数仍是 66**、人格权重一条没动。
- **建立唯一权威的图遍历入口**：`isPassage(sc)` + `nextTargets(sc)` 从 `scenes.js` 导出，verify / simulate / branch_audit / trace_choices / psy_audit **全部改走它们**。

  > 为什么必须统一：本轮真实踩到两次"某个工具不知道新节点类型存在"的**静默漏检**。
  > `psy_audit.mjs` 自己的遍历没实现过场处理，`availableIdx` 返回空数组 → 循环立刻 break → 一局只走一步，
  > 输出变成「有效题量均值 1.0 / 1、一致率 50.0%、NaN 个百分点」；
  > `smoke_gate.py` 的 `PICK_JS` 直接 `sc.choices.map(...)`，在过场节点上抛 `TypeError`。
  > 两个工具都不是"报错"，是**给出了看起来正常的错数字**。

- **修掉的 5 类文案缺陷**（都是新的结构暴露出来的）：
  1. `x_task_lone` 结尾「身后没有人。」↔ 汇合点插段「你身后一个人也没有。」—— 换了说法的重复
  2. `x_gela_power` 选了「亮靖安司腰牌」，过场却从「你办完事」写起，**把玩家刚点的动作跳过去了**
  3. `h_zi2_gela` / `h_chou` 的插段在重播过场已经演过的告别（且"茶是谁喝的"互相打架）
  4. `x_doc_confront` 照抄上游 `h_si_doc` 正文 **34 个字**
  5. `x_tail_help` 断言「你身上没有腰牌，也没有官凭」，与 `x_doc_confront` 的「把靖安司的令牌亮出来」在同一时间线上对立
  - 附带：`h_wu` 选项 4 原本让崔器把「边军冬衣」念第三遍（基座正文念过、选项里又念）→ 改成摊开**两张单子对不上**，信息往前推一步
  - 顺手修掉错字「李必胜不讳」→「李必并不讳言」（2 处）

- **新增工具 `tools/passage_audit.mjs`**（`npm run audit:passage`）：把每段过场与**上游抉择点 / 汇合点 / 两处的条件插段**逐一比对，用**最长公共子串**抓照抄、用**三元组重合系数**抓"换了个字"的重复。
  它第一版只比下游汇合点，结果漏掉了第 4 条——**过场最容易复述的不是下一场戏，是上一场戏**。这个盲区是被人工通读逼出来的。
  `digest` 模式打印紧凑清单（过场正文 + 汇合点开头），供逐对人工通读。

- **`verify` 新增：成对符号配对检查**（`「」` `“”` `（）` `《》`）。少一个引号浏览器照样渲染，玩家只觉得读着怪，**只会静默上线**。

- **新增 `tools/shot_passage.py`**（`npm run shot:passage`）：走到第一个过场、截图、再点一下「继续」确认真的跳到 `s1_meet`。
  结构测试只能证明按钮存在且可点，证明不了它看起来像不像"该点一下"。

- **`branch_audit` 的「主干」定义重写**：旧定义是"顺着唯一出口走、遇多出口即停"——过场铺开后每个抉择点都有多个去处，这个定义会在第一个节点就停（报"主干 1 个节点"）。改成从数据算：**「删掉它之后，没有任何一个结局还能到达」= 不可绕过**。现在算出 19 个不可绕过节点 + 4 个结局，不再写死。

- **`simulate` / `psy_audit` / `dump_play` 的写死步数上限全部改成从数据算**（`tools/_browser.py` 新增 `max_steps(page)`）。
  写死的 `range(30)` / `range(40)` / `maxSteps=60` 在节点数从 26 涨到 85 之后全部不够用（`smoke.py` 直接报「结局浮层: False」）。**目录结构一变，写死的数字就成了定时炸弹。**

- **`dump_play.py` 的 `seq` 序号口径**：只数**抉择点**，过场不消耗序号（用 `cp` 计数）。

**回归结果**：`verify` ✅ 0 错误（85 节点 / 59 过场 / 66 选项）· `check:readme` ✅ 14/14 · `simulate` ✅ 4/4 结局可达 · `branch_audit` ✅ · `passage_audit` ✅ 0 处复述 · `psy_audit` ✅ · **6 个**浏览器冒烟测试 ✅

**支路饱满度**（`verify` 新增小节）：

```
21 / 22 个抉择点：每个选项的去处互不相同
其中 20 个是"先走一段专属过场，再汇合回主干"
仍有多个选项通向同一处的 1 个：h_hai（4 选项 → 3 个去处）
```

`h_hai` 是唯一例外，而且**是设计上的正解**：它的选项 3/4 都通向 `e_final_persuade`，靠互斥开场插段（`flagsAll: ['final_empathy']` / `flagsNone`）区分两条来路——这是 v5.3 修掉的"一个出口两条来路共用开场"，不是缺陷。

### v5.3 (2026-09-11) — 「选完之后，紧接着读到什么」

起因：玩家追问「**目前的选择连贯吗**」。v5.2 已经证明了"结构上连得起来"（flag 有人读、结局可达、没有死标记），但那只回答了"选择**有没有**后果"，没回答"**选完之后那一段，是不是真的接住了这个选择**"。

于是新写了一个工具 `tools/trace_choices.mjs`，把 22 个选择节点逐条摊开：**每个选项 → 写到哪个 flag → 目标节点 → 该选项真正命中的那段插段 → 后文有没有引用这个 flag**。然后人肉读完 66 个选项的「紧接着读到」。

结论：**接线是通的**（66 个 flag 里 62 个被后文读取，0 个真死标记；三条主要走法的收尾确实不同），**但有 3 处接不住**，全部修掉：

1. **门槛卡在两个可达值中间，而提示描述的是走不通的那条**（`h_hai` 选项 4）
   选项要求 `trust.longbo >= 30`，但龙波初始信任 0，全剧只有两处给他加信任，且**都在 `h_xu` 里互斥**：
   · `hear_longbo`「你恨的到底是什么？说清楚。」 **+20**
   · `sympathize`「长安不是欠你的。但你的仇，我记下了。」 **+35**
   而 `requireHint` 写的是「需要先在灯楼听懂他恨的到底是什么」—— 描述的正是 **+20** 那条。
   玩家照着提示走，信任变成 20，**门还是锁着**。
   → 门槛改成 **20**。两条"理解他"的路这才都真的走得通（已实测：+20 解锁 ✅、+35 解锁 ✅、0 保持锁定 ✅）。

   这类 bug 的可怕之处是"看起来门槛是可达的"（35 ≥ 30），静态检查很容易放过。
   为此给 `verify` 加了一条**新判据**：**门槛卡在两个可达值中间**就报警，并直接列出"哪条够、哪条不够"，逼你回头确认 `requireHint` 描述的是"够"的那条。

2. **一个出口有两条来路，开场却只写了一条**（`e_final_persuade`）
   `h_hai` 的第 3、4 选项都通向 `e_final_persuade`，但结局正文第一句是
   「**「龙波，」你说，「我替你把仇报了。」**」—— 这是第 3 个选项的台词。
   选了第 4 个选项（「我懂你。但你杀的不是仇人。」）的玩家，会读到自己**从来没说过**的一句话。
   → 拆成两条互斥开场插段：`flagsAll: ['final_empathy']` 走"理解"版，`flagsNone: ['final_empathy']` 兜底走"承诺"版。
   `final_empathy` / `final_persuade` 在 `h_hai` 里天然互斥，所以 `flagsNone` 能安全兜底。

3. **后文紧接着做了一个你刚拒绝的动作**（`h_si_doc` 选项 3）
   选「现在不是说这个的时候。长安要没了。」之后，基座正文下一句是「**你翻开文书。**」
   基座正文对三条来路**不是都成立**：
   · 选 1「接过文书，当场翻开」→ 成立，而且正是这个选择的动作
   · 选 2「我给你一个交代」→ 她推过来，成立（插段已交代）
   · 选 3「现在不是说这个的时候」→ 你刚拒绝，下一句自己翻开 = 硬拐弯
   → 给 `cold` 插段补了过渡：她没收回手，把文书往你这边推了半寸 ——「推过来的东西，你不接，它就一直在那儿。」

**顺便修正：`trace_choices` 自己的输出曾经在骗人。** 它早先只看得懂 `flagsAll` / `flagsAny`，读到 `flagsNone` 兜底插段时就跳过，于是把 `e_final_persuade` 的"基座"显示成第 4 句——看起来像"选项 3、4 读到同一段"。其实它压根没模拟条件。改成三值逻辑（`true` / `false` / `null = 取决于前面的玩法`）之后，才正确显示选项 3 → `ins#1`、选项 4 → `ins#0`。
**审计工具本身也会撒谎** —— 工具报"全绿"的时候，得先确认它真的看懂了数据。

**新增工具**：`node tools/trace_choices.mjs`（`--brief` 只看摘要，或 `node tools/trace_choices.mjs h_hai` 只看某节点）

**回归结果**：`verify` ✅ 0 错误 · `check:readme` ✅ 13/13 · `simulate` ✅ 4/4 结局可达 · `branch_audit` ✅ · `psy_audit` ✅ · **6 个**浏览器冒烟测试 ✅

### v5.2 (2026-09-11) — 系统性复查：把"跨节点的矛盾"找出来

起因：v5.1 之后做了一轮全项目系统性检查（跑全部自动化 + 逐条支路人肉通读 + 交叉核对代码/数据/文档/工具）。

这一轮共抓到 **4 类叙事/元数据缺陷 + 1 个页面级硬故障**。前 4 类的共同点是：**单看任何一段都通顺，只有在整条线上读、或跨字段比对时才暴露。**

1. **`h_hai` 与 `e_burn_fire` 正面冲突（最严重）**
   `h_hai` 正文写「楼下传来脚步声——不止一个人」「**他们全都上来了**」，
   但它能走到 `e_burn_fire`，而那个结局开场就是「**但你身后没有人。灯楼上只有你一个。**」
   玩家会连着读到这两句，剧情当场塌掉。
   → 正文改为不表态的写法（只写"楼下传来脚步声"），到底谁来了交给结局去点名。
   同时在节点里写清约束：**`h_hai` 是全局唯一分流点，之前的 22 个节点全部能到达全部 4 个结局。**

2. **结局引用了从未出现过的台词**
   `e_final_people` 写「你想起闻染说的话——长安不用你守」，
   但检索全库发现「长安不用你守」**只出现在这一行**，闻染从未说过。
   → 改为不依赖引用的收束：「长安其实不用谁去守。要守，就守身边的人。」

3. **侧栏「当前视角」在 6 个节点显示错误角色**
   `s1_meet` / `s1_task` / `h_chou` / `h_chen` / `h_wu` / `h_wei` 标着李必或崔器，
   正文却都是张小敬第二人称（「李必从屏风后走出来」「李必的声音在抖」），
   玩家会看到侧栏在"张小敬/李必/崔器"之间无理由乱跳。
   → 全部改正为真实叙述者，并新增自动判据：**pov 角色的名字出现在正文里 ⇒ 叙述者不可能是他**（列为 error）。

4. **`h_shen` 的伤口描写与其中一条支路矛盾**
   正文写「伤口在流血，你已经不记得是第几道了」，
   但从 `h_wei` 的 `observe`（先看是谁下的令）那条路过来时，你根本没动手，
   上一节点还明说「没有人追你」。
   → 拆成两条互斥插段（`flagsAny: ['breakout','decoy']` / `flagsNone: [...]`），有伤/没伤各说各的。

另外两处小修：`h_mao` 的 `follow_track` 插段里「他回头看了一眼」与紧接的正文「正快步穿过人群」打架且换主语 → 删掉该句；
`h_xu` 的 `h_mao_snack` 一类指代不清的句子一并清理。

**附带发现：`results.html`（结局与人物图鉴页）一直是白屏的**

这是本轮最"重"的一个 bug，和叙事无关，纯粹是页面根本跑不起来：

```js
// describeCond 是函数声明（会提升），但它在顶层 `const endings = ...` 求值时被同步调用
const endings = Object.entries(scenes)... routes: routesTo(id)   // ← 这里就调用了 describeCond
...
const flagLabel = (f) => FLAG_LABELS[f] || f;                    // ← const 不提升，声明在 400 行之后
```

→ 命中 TDZ，页面抛 `Cannot access 'flagLabel' before initialization`，**4 张结局卡、16 张 MBTI 卡、16 张人物卡一张都不渲染**。

它之所以能藏这么久：`verify` / `simulate` / `branch_audit` / `psy_audit` 全都只 import `src/data/*.js`，
**从不加载任何 HTML 页面**；而 `smoke.py` 等 5 个浏览器测试只测 `index.html`。
CSS 正常、HTML 结构正常、`index.html` 全绿 —— 没人看这个页面，它就一直是白的。

- → 把 `FLAG_LABELS` / `flagLabel` 提到 `endings` 之前声明
- → 新增 `tools/smoke_results.py`（`npm run smoke:results`）：断言页面真的渲染出 4 / 16 / 16 张卡片、
  没有 `{{inserts}}` 泄漏、没有运行时错误。**"页面能跑"本身必须被测。**

**工具链的加强（比修 bug 更重要）**

- **`simulate.mjs` 的叠加审计有漏洞**：循环条件写着 `!scenes[st.sceneId].ending`，
  导致审计**在结局节点前就停了**——而结局恰恰是插段最密的地方。
  修好后 `e_final_people` 立刻暴露出来（5 条同时生效），README 里那句"最多 2 条"原本是错的。
- **新增"节点已声明叠加"机制**：`stackAck: '理由'`。审计报出无条件+条件叠加时，
  若节点已声明理由就打印 `✔ 已知叠加`，否则 `⚠️`。
  不写理由直接忽略，等于把这个警告变成永远亮着的红灯。
- **新增 `dump:endings`**：4 个结局 × 2 个极端（同路人拉满 / 一个都没有）各打印一遍，
  并断言渲染结果无 `{{inserts}}` 残留。
- **修掉 `{{inserts}}` 泄漏**：`showEnding()`（`src/main.js`）和 `results.html` 都直接读 `sc.text`，
  绕过了解析逻辑 —— 结局一旦有插段就不渲染，还会把 `{{inserts}}` 标记原样显示给玩家。
  → 把整套条件正文解析抽到 **`src/text.js`**，引擎与静态页共用同一份实现（同一份规则只能有一个实现）。
- **新增 `verify` 校验**：「谁在场」跨结局一致性（启发式，短语表）、`pov` 与叙述者一致性、分流点分布报告。
- `results.html` 的结局正文改为按空行分段渲染（原先整段塞进一个 `<p>`，多段被压成一坨）。

**回归结果**：`verify` ✅ 0 错误 · `check:readme` ✅ 13/13 · `simulate` ✅ 4/4 结局可达 · `branch_audit` ✅ · `psy_audit` ✅ · **6 个**浏览器冒烟测试 ✅

### v5.1 (2026-09-11) — 让插段落在结构正确的位置

起因：上一轮修完之后玩家仍然反馈「点选导致的剧情不连贯，还是没法玩儿」。

原因不在"有没有条件正文"，而在**插段插在了哪儿**。v5 的 `pos` 只能二选一（塞最前 / 塞最后），
而节点正文都以转场句开头，于是全部读成了预告式倒叙：

```
你还没去坊里，葛老却像早算到你会路过这儿。   ← 插段被塞在最前
「你要找的那姑娘，」他说……

你出了靖安司，没有直接去查案。              ← 正文的转场句
你绕进了一条暗巷——平康坊的地下暗市。
```

结构测试全绿（节点连通、flag 都被消费、19 个共同节点里 18 个正文不同），但**读起来是断的**。

- **引擎**：`resolveText` 改为按正文里的 `{{inserts}}` 标记定位，删掉 `pos` 字段（61 处）
- **内容**：给 17 个节点逐一确定插入点，并重写那些写成了"预告"的插段，让它们改成"承接"
  - 关键原则：**标记落在"已经交代清楚你在哪"之后**，插段落在标记之后必须接着上一句往下走
- **修掉 3 处真实矛盾**：
  1. `h_xu`：「十七桶」与「你只砸开了八只，剩九只」会同时插入 → 把桶数挪到叙述层，三种情况互斥
  2. `h_chen`：`reported` 插段写「你昨晚就报回去了」，但该路径上根本没回报过 → 改为按实际路径描述
  3. `h_mao_snack`：`tailed`（吊着他）排在 `try_destroy`（砸罐）之后，读成倒叙 → 整个数组按时间重排
- **新增审计**：`simulate.mjs` 加入**插段叠加审计**——随机 3000 局枚举"哪些插段会一起出现"。
  文字层面的矛盾（「十七桶」vs「只剩九只」）静态检查抓不到，只能这样枚举。当前全剧最多 2 条同时生效。
- **新增审阅工具**：`tools/dump_play.py` 把一局完整流程按**玩家视角**打印成可读文本，
  支持 `first` / `last` / `random <seed>` / `seq <序号表>`。**脚本测不出"读起来通不通"，只能看。**
- **`verify` 新增校验**：有插段的节点必须写 `{{inserts}}` 标记；无条件插段会与条件插段叠加时给警告
- 顺手修掉 `psy_audit.mjs` 里写死的题量分母（`/ 64` → 跟着数据走）

### v5 (2026-09-11) — 让每一次选择都留下痕迹

起因：玩家反馈「选了『我凭什么听你的』，结果还是跟着去了靖安司，后文完全没反应」。审计后发现 91% 的节点是伪选择、97% 的 flag 只写不读。

- **新增条件正文机制**（`src/main.js` 的 `resolveText`）：
  - `inserts: [{ when, text }]` —— 段落插入，可叠加，**不复制原文**（首选）
    （⚠️ 定位方式在 v5.1 里从 `pos: 'start'|'end'` 改成了正文里的 `{{inserts}}` 标记）
  - `textVariants: [{ when, text }]` —— 整段重写（只用于基调完全改变的场合）
- **内容接线**：61 条插段覆盖 17 个节点（v5.1 给 `h_xu` 补 3 条、v5.2 给 `h_shen` 补 2 条、`e_final_people` 补 5 条、v5.3 给 `e_final_persuade` 拆开场 + `h_si_doc`/`h_shen` 补桥；截至 v5.5 共 **75 条 / 19 节点**），让每段剧情按你的来路读起来不同
  - 例：死牢选「我凭什么听你的」→ 靖安司那段会写你「在门槛上站了半息」，李必说「你在掂量我」
- **新增 2 个条件选项**：`s1_meet` 第 4 选项（需 `cautious`）、`h_hai` 第 4 选项（v5.3 起需龙波信任 ≥ 20，原先的 30 卡在两条可达值中间）
- **新增条件路由**：`try_destroy`（西市毁油罐）→ 同路人不足也能守住长安，让**中段选择真的改变结局**
- **死标记清零**：62/64（97%）→ **0/66（0%）**；无痕迹节点 20/22（91%）→ **0/22（0%）**
- **结局分布更均衡**：未竟之局 13.7% → 24.1%（多了一条「以情动人」路线）
- **审计工具升级**：`branch_audit.mjs` 判定改为三档（真分叉 / 有痕迹 / 无痕迹），并把结局分支上的「终局标记」排除出死标记统计；新增 `smoke_text.py` 用真实浏览器比对两条走法的渲染文本（当前 19 个共同节点里 18 个不同）

### v4 (2026-09-11) — 让选择真正影响结局

- **条件系统**：新增 `require` / `requireHint` 选项门控与 `{ when, then }` 条件路由，支持 `flagsAll` / `flagsAny` / `flagsNone` / `trust` / `allies` / `clues` 六类条件键
- **新增第 4 结局「火起长安」**：拔刀但同路人不足时触发，让「独行到底」付出代价
- **MBTI 计分重写**：经验公式 → 机会基线归一化，四维分布从 E80.5%/T16% 收敛到 47–53%，16 型全部可达
- **平局显式化**：修复「进度条 50/50 却硬判右侧」的自相矛盾，新增「无偏好」标注
- **条件选项不计入满分**：`nodeMax` / `mbtiNodeStats` 按实际可选下标归一化
- **存档**：`localStorage` 自动存档 + 续玩提示 + 静默降级
- **移动端**：侧栏 `display:none` → 抽屉式侧滑
- **无障碍**：键盘选择、焦点管理、`aria`、`prefers-reduced-motion`
- **新增 `results.html`** 结局图鉴（此前结局文案在页面里硬编码且与实际数据不一致）
- **测试基建**：新增 `simulate.mjs`（免浏览器 3000 局回归）、`psy_audit.mjs`（心理测量学审查）、`smoke_gate.py`（门控集成测试）、`_browser.py`（浏览器探测 + 代理绕行 + 存档清理）、`package.json`、`tools/requirements.txt`
- **可移植性**：移除硬编码的 Chrome 路径；移除废弃的 `tools/smoke.mjs`
- **清理**：删除死导出 `MBTI_DIM_MAP`，修正残留英文串与传统字，`.gitignore` 覆盖全部测试截图

### v3 (2026-09-09) — MBTI 人格测评

- 新增 `src/data/mbti.js`（4 维 MBTI + 抉择权重 + 16 型定义 + 16 角色向量）
- 角色从 13 位扩充到 **16 位**，新增丁瞳儿(INFP) / 王韫秀(ENFJ) / 葛老(ESFP)
- 修正徐宾→INTP、崔器→ISTJ，使 **16 型 ↔ 16 人物一一对应、零重复**
- 引擎累计 MBTI 分，结局页新增 MBTI 区块（双向百分比条 + 类型 + 称号 + 三层匹配人物卡）
- 长安六维降级为辅助补充画像
- 新增 `tools/smoke_mbti.py`、`tools/mbti_dist.py`

### v2 (2026-09-09) — 长安六维

- 新增 `src/data/traits.js`（6 维 + 抉择权重 + 13 角色人格向量）
- 结局页新增「长安人格」画像区段（条形图 + 主导特质解读 + 最相似角色）

### v1 (2026-09-09) — 初始版本

- 十二时辰全覆盖，3 结局连通
